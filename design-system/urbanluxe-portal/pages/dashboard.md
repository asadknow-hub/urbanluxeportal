# Dashboard — overrides

- Bento, not a uniform 8-up of identical cards
- Hero tile: pipeline value (2 cols)
- Revenue + overdue as contrast tiles (overdue uses destructive, not gold)
- Activity (2/3) + follow-ups (1/3)
- Quick actions as compact chips on the page header, not a gradient banner
- Same data queries as before
